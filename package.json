{
  "name": "pixel-editor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "base": "/pixel-editor/",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.0",
    "vite": "^5.0.0"
  }
}
